# Nixpacks configuration for Railway
# Adiciona FFmpeg ao ambiente

[phases.setup]
nixPkgs = ["ffmpeg-full", "python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}"
